{
  "id": "invaders-score",
  "title": "Give each alien a score",
  "description": "The hit alien already disappears. Add points inside on_hit().",
  "hint": "Add a positive number to world.score after alien.hide().",
  "starter": [
    "# Move the ship, fire at the formation, and clear all 21 aliens.",
    "ship.speed = 5",
    "world.alien_speed = 0.65",
    "world.sky = \"night\"",
    "",
    "def move_ship():",
    "    if keyboard.left:",
    "        ship.x -= ship.speed",
    "    if keyboard.right:",
    "        ship.x += ship.speed",
    "",
    "def fire_laser():",
    "    if keyboard.fire:",
    "        ship.fire()",
    "",
    "def update():",
    "    move_ship()",
    "    fire_laser()",
    "",
    "def on_hit(alien):",
    "    alien.hide()",
    "    pass"
  ],
  "guide": {
    "function": "on_hit",
    "replace": "pass",
    "instruction": "Add a positive number to world.score after alien.hide().",
    "review": "Run your rule, then use Check my step."
  }
}
